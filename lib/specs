*asm:
-mthumb-interwork

*cc1:
-I%:getenv(PREFIX /include) -mcpu=arm7tdmi -mthumb -mthumb-interwork -ffreestanding

*cc1plus:
%(cc1) -fno-exceptions -fno-rtti

*link:
-L%:getenv(PREFIX /lib) -Tlnkscript --orphan-handling=error -gc-sections

*libgcc:
-lgcc

*lib:
-lgccmem

*startfile:
-l:crt0.o -l:crti.o

*endfile:


